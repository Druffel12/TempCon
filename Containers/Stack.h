#pragma once
#include "tvector.h"
#include "intlinkedlist.h"

template <typename T>
class Stack
{
Private:

		tVector<T> vector;

public:
	//empty
	//size
	//pop
	// - return a what was popped
	//push
	//- add element to the top of the stack
	//Top
	// - access top of the stack

};

template<typename T>
inline int * Stack<T>::top() const
{
	return NULL;
}
